dist:     trusty
sudo:     required

language: cpp

os:
  - linux

compiler:
  - gcc

addons:
  apt:
    update: true
    packages:
      - cmake
      - cppcheck
      - valgrind

script:
  - ctest ./tests
  - cppcheck -q --enable=all ./src